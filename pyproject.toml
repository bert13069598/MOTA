[project]
name = "mota"
version = "0.2.0"
description = "Add your description here"
requires-python = ">= 3.14"
dependencies = [
    "torch",
    "tqdm",
    "ultralytics",
    "motmetrics",
    "opencv-python"
]
